The `run()` function accepts an [`ArgumentParser`](https://docs.python.org/3/library/argparse.html#argparse.ArgumentParser) directly, so you can migrate an existing argparse-based program with minimal effort. Be aware of a few [caveats](Argparse-caveats.md) for advanced CLI patterns.

::: mininterface.cli
